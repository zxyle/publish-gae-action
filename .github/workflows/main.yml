on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Pushlish project to Google App Engine
    steps:
    - name: Hello world action step
      uses: zxyle/pushlish-gae-action@1.1

    - name: Get gcloud version
      run: gcloud version